'use client';

import { FC } from 'react';
import { SliderServices } from '@/components/modules';

const Services: FC = () => {
  return (
    <section id="services">
      <SliderServices />
    </section>
  );
};

export default Services;
