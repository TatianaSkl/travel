'use client';

const Hero = () => {
  return (
    <section className="section hero-bg-m m:hero-bg-t l:hero-bg-d">
      <div className="container">
        <h1></h1>
      </div>
    </section>
  );
};

export default Hero;
